# JCDecaux Purchase Order Report

Odoo 17 add-on that provides the JCDecaux purchase order PDF layout.

The first implementation covers the report and its global terms configuration.
Approval history integration is intentionally deferred until the source approval
module and its technical fields are confirmed.

